# Same-host performance report — 4.0.2 vs TS 3.0.14

Status: **historical startup-inclusive measurements retained; formal product admission reopened**  
Generated: 2026-07-24T12:27:00Z (original suite)  
Audit: 2026-07-24 post-publish independent review  
Candidate SHA (historical suite): `18514b56a974062bf5cd24004262292469728975`  
Baseline: `@sylphx/pdf-reader-mcp@3.0.14`

## Current product stance

- Sole-Rust packaging/runtime for **4.0.2** is live and verified.
- Formal marketing speedup claims are **withheld**.
- Rebuilt harness (`scripts/perf/*`, contract `same-host-ab-contract.md`) separates:
  - `startup_inclusive` (spawn + initialize + task)
  - `persistent_warm` (long-lived process, tools/call only)
- Latest draft suite status: `measured_draft_not_admissible` (see `verification/pdf-reader-same-host-ab-suite-draft.json`).

## Historical method (superseded for formal claims)

- Task: MCP initialize + `read_pdf` page 1 with `include_full_text`
- Each sample effectively re-spawned the process (startup-inclusive end-to-end)
- Warm iterations: 7
- Semantic gate: successful non-trivial payload length (weak)
- Engine order randomized for cold and warm runs

## Historical aggregate (diagnostic only)

- Required classes all `fixture_pass` under the old gate: **yes**
- Min warm median speedup (TS/Rust): **6.25x** (startup-inclusive)
- Median of class speedups: **23.69x** (startup-inclusive)

## Historical per-class warm median latency

| Class | Status | TS median ms | Rust median ms | Speedup |
| --- | --- | ---: | ---: | ---: |
| small_text | fixture_pass | 301.2 | 13.9 | 21.61x |
| structured | fixture_pass | 281.9 | 10.0 | 28.30x |
| table_heavy | fixture_pass | 290.4 | 24.7 | 11.78x |
| geometry_edge | fixture_pass | 280.9 | 11.9 | 23.69x |
| metadata_structured | fixture_pass | 289.3 | 8.2 | 35.25x |
| text_segmentation | fixture_pass | 274.2 | 10.9 | 25.14x |
| behavior_baseline | fixture_pass | 282.4 | 17.4 | 16.24x |
| hostile_table_bound | fixture_pass | 363.1 | 58.1 | 6.25x |

These numbers remain useful diagnostics for MCP spawn/start path cost. They are **not** authorized as steady-state warm `read_pdf` product claims.

## Package size (historical run)

> **Do not** market “92 KB TS vs 25 MB Rust executable” as product size. See
> [installed footprint comparison](./installed-footprint-comparison.md).

## Installed product footprint

See raw suite packageSizeBytes samples (TS 3.0.14 tarball ~92 KB; sole-Rust launcher pack ~16 KB; native binary ~25 MB).

## Limitations / audit findings

1. Warm samples re-spawned the process each iteration (startup-inclusive, not persistent warm).
2. Semantic gate was response-length only; not normalized text / task-outcome comparison.
3. Class labels did not run capability-specific tasks.
4. Raw per-iteration samples were gitignored; only summary committed.
5. Suite bound local/release binaries, not registry-installed exact 4.0.2 natives.
6. Peak RSS unavailable on that runner.

## Path to formal re-admission

1. `persistent_warm` fixture_pass on all required classes under rebuilt contract
2. Registry-installed exact natives (`MCP_PDF_PERF_RUST_FROM_REGISTRY=1`)
3. Durable raw samples under `verification/perf/raw/`
4. Honest published report with method bounds
5. Successor independent review with `performanceClaimsAuthorized=true`

## Evidence

- Historical suite: `verification/pdf-reader-same-host-ab-suite-4.0.2.json`
- Draft rebuilt suite: `verification/pdf-reader-same-host-ab-suite-draft.json`
- Claims policy: `docs/specs/performance/4.0.2-performance-claims-policy.md`
- Contract: `docs/specs/performance/same-host-ab-contract.md`
