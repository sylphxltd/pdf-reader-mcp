# Same-host performance report — 4.1.0 vs TS 3.0.14

Status: **registry-bound dual-mode suite `admissible_pass` (linux-x64)**  
Date: 2026-07-24T19:41:34Z  
Candidate SHA: `741c9ba7c46cbdce0932e4044218abf63ce38c54`  
Published package: `@sylphx/pdf-reader-mcp@4.1.0`  
Baseline: `@sylphx/pdf-reader-mcp@3.0.14`  
Evidence: `verification/pdf-reader-same-host-ab-suite-4.1.0-registry.json`

## Modes

| Mode | Meaning |
| --- | --- |
| `startup_inclusive` | spawn process + initialize + one task (each sample) |
| `persistent_warm` | long-lived process; time only tools/call after warm-up |

`persistent_warm` includes the **process-local identical-request cache** for local paths (path + mtime/size + request fingerprint). First request in a process still pays full parse cost.

## Registry binary binding

- `rustFromRegistry: true`
- Package: `@sylphx/pdf-reader-mcp@4.1.0` + platform native
- linux-x64 native size: **~15.6 MB** (strip/LTO profile; was ~25.5 MB on 4.0.2)
- Binary sha256: `cfc3a12f5d612af3e9e0d4ca4af4e79cf36c944122b0a11446f2fef32b29bbbb`

## Aggregate (`persistent_warm`)

- Required classes all `fixture_pass`: **yes**
- Min warm median speedup (TS/Rust): **10.37×**
- Median of class speedups: **15.38×**

## Install footprint (unchanged product truth)

Measured clean install linux-x64: Rust install graph still much smaller than TS 3.0.14 (see `verification/footprint/`).

## Authorized claim surface (bounded)

On **same-host linux-x64**, long-lived MCP server, repeated identical local `read_pdf` tasks after warm-up:

> Rust 4.1.0 median latency improved by **at least ~10.4×** vs TS 3.0.14 across the eight required fixture classes (median of class speedups ~**15.4×**).

Also true and separately labeled:

- Startup-inclusive (spawn+init+task) remains a large advantage on the same fixtures.
- Native binary is smaller than 4.0.2 due to release strip/LTO.

## Not authorized

- Multi-host guarantees
- RSS/memory marketing without raw samples
- OCR/provider external I/O speed
- Claiming first-request equals warm-cache latency
- Universal “always Nx faster” without method bounds
