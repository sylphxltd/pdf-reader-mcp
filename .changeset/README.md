# Changesets

This directory stores release intent files for Changesets.

Add one changeset for each user-visible package change. The Release workflow
turns merged changesets into a version PR, then publishes after that version PR
is merged.
